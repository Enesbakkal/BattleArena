# Copy to .env in this folder (same level as docker-compose.yml).
# Compose API → host SQL (MSSQL2024). Enable TCP/IP + static port in SQL Configuration Manager first.
SQL_SERVER_HOST=192.168.1.124
SQL_SERVER_PORT=14330

RabbitMq__Enabled=true
RabbitMq__HostName=rabbitmq
ConnectionStrings__Redis=redis:6379
